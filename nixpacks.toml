[providers]
python = "3.11"
node = "20"

[phases.install]
cmds = ["pip install -r backend/requirements.txt", "npm install -g @mermaid-js/mermaid-cli"]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
