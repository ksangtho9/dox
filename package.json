{
  "name": "dox-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix dox",
    "build": "npm run build --prefix dox",
    "start": "npm run start --prefix dox",
    "lint": "npm run lint --prefix dox"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "framer-motion": "^12.34.3",
    "lucide-react": "^0.575.0",
    "tailwind-merge": "^3.5.0"
  },
  "devDependencies": {
    "tw-animate-css": "^1.4.0"
  }
}
